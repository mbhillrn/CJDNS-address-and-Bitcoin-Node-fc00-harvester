# lib

Library code used by the CJDNS Bitcoin Core address harvester.

Contains reusable modules (primarily under `lib/harvest/`) that implement ingest, probing,
onetry dispatch, confirmation, and database helpers.
